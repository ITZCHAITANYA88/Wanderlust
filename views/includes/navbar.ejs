<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top shadow-sm">
  <div class="container-fluid">
    <!-- Brand -->
    <a class="navbar-brand fw-bold text-primary" href="/listings">
      <i class="fa-regular fa-compass me-1"></i> Wanderlust
    </a>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Left nav -->
      <div class="navbar-nav me-auto">
        <a class="nav-link" href="/listings">Explore</a>
      </div>

      <!-- Search bar -->
      <form class="d-flex align-items-center me-md-3 mb-2 mb-md-0 search-form" action="/listings/search" method="GET">
        <div class="input-group shadow-sm rounded-pill">
          <input type="search" name="q" class="form-control border-0 rounded-start-pill ps-4" placeholder="Search destinations..." aria-label="Search">
          <button class="btn btn-search rounded-end-pill px-4" type="submit">
            <i class="fa-solid fa-magnifying-glass me-1"></i> Search
          </button>
        </div>
      </form>

      <!-- Right nav -->
      <div class="navbar-nav ms-auto align-items-md-center flex-column flex-md-row text-center">
        <a class="nav-link btn btn-outline-primary mb-2 mb-md-0 ms-md-2" href="/listings/new">Airbnb your home</a>

        <% if (!currentUser) { %>
          <a class="nav-link" href="/login"><b>Login</b></a>
          <a class="nav-link" href="/signup"><b>Sign Up</b></a>
        <% } else { %>
          <span class="nav-link disabled">Hello, <%= currentUser.username %>!</span>
          <form class="d-inline" method="POST" action="/logout">
            <button type="submit" class="btn nav-link border-0 bg-transparent text-danger">Logout</button>
          </form>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<!-- Custom styling -->
<style>
  .search-form {
    max-width: 600px;
    width: 100%;
    transition: all 0.3s ease;
  }

  .search-form .input-group {
    background-color: #fff;
    border-radius: 50px;
    overflow: hidden;
    transition: box-shadow 0.3s ease;
  }

  .search-input {
    height: 52px;
    font-size: 1.1rem;
    padding-left: 1.2rem;
    border: none;
    outline: none;
    transition: all 0.4s ease;
    background-color: transparent;
  }

  .search-input:focus {
    box-shadow: 0 0 0 3px rgba(250, 66, 77, 0.2);
    background-color: #fff;
  }

  .search-form:focus-within .input-group {
    box-shadow: 0 0 10px rgba(250, 66, 77, 0.4);
    transform: scale(1.02);
  }

  .btn-search {
    background-color: #fa424d;
    color: white;
    border: none;
    transition: background-color 0.3s, transform 0.2s, box-shadow 0.2s;
    font-size: 1.05rem;
    padding: 0.6rem 1.4rem;
  }

  .btn-search:hover {
    background-color: #c8323b;
    transform: scale(1.05);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
  }

  @media (max-width: 768px) {
    .search-form {
      width: 100%;
      margin: 0.75rem 0;
      order: 2;
    }

    .navbar-nav.ms-auto {
      order: 3;
    }
  }
</style>
